kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 31113   # NGINX gateway µBench
        hostPort: 31113
        protocol: TCP
      - containerPort: 30000   # Prometheus
        hostPort: 30000
        protocol: TCP
